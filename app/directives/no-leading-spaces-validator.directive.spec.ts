import { NoLeadingSpacesValidatorDirective } from './no-leading-spaces-validator.directive';

describe('NoLeadingSpacesValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NoLeadingSpacesValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
